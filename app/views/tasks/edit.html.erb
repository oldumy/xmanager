<%= content_for :menus do
  menus(current_user, :sprint_backlogs, "/projects/#{@task.product_backlog.project.id}")
end %>

<div class="form-wrapper">
  <h1><%= t("headers.forms.edit_task") %></h1>
  <%= render :partial => 'form', :locals => { :task => @task } %>
</div>
